add_library(obd STATIC src/J1979.cpp src/CanChannel.cpp src/Poller.cpp src/IsoTp.cpp src/Uds.cpp src/J1979Client.cpp)
add_library(canproxy::obd ALIAS obd)
target_include_directories(obd PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
set_target_properties(obd PROPERTIES CXX_STANDARD 17 CXX_STANDARD_REQUIRED ON POSITION_INDEPENDENT_CODE ON)
target_compile_options(obd PRIVATE -Wall -Wextra -Werror)

if(CANPROXY_BUILD_TESTS)
    foreach(t j1979_tests poller_tests uds_tests)
        add_executable(${t} tests/${t}.cpp)
        target_link_libraries(${t} PRIVATE canproxy::obd)
        set_target_properties(${t} PROPERTIES CXX_STANDARD 17 CXX_STANDARD_REQUIRED ON)
        target_compile_options(${t} PRIVATE -Wall -Wextra -Werror)
        add_test(NAME ${t} COMMAND ${t})
    endforeach()
endif()
